#include <iostream>
/*Gist 12: write a function that multiplies two numbers.
You can only use addition or subtraction and you can only check whether a number is positive negative or 0.
Can not use > >= < <=*/
//unsigned int mult(unsigned int num1, unsigned num2) {
//	unsigned  answer = 0;
//
//	for (int i = 0; i != num2; ++i)
//		answer += num1;
//	return answer;
//}
//
//int main() {
//	std::cout << mult(10, 5) << std::endl;
//	system("pause");
//}